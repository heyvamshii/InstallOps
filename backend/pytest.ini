[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = .
python_files = test_*.py
testpaths = apps
addopts =
    --strict-markers
    --cov=apps
    --cov-report=term-missing
markers =
    unit: pure domain logic, no HTTP
    integration: exercises the API surface
